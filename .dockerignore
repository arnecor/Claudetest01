# Dependencies – reinstalled in the image
node_modules

# Next.js build output – rebuilt in the image
.next
out

# Git
.git
.gitignore

# Secrets – injected at runtime via docker-compose environment
.env
.env.local
.env.*.local

# Editor / OS
.vscode
.DS_Store
*.pem

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript incremental cache
*.tsbuildinfo

# Prisma generated client – regenerated during docker build
app/generated

# Docker files themselves (entrypoint script IS copied into the image)
Dockerfile
docker-compose.yml
.dockerignore

# Misc
coverage
.claude
README.md
